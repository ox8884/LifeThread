# Local browser demo requires no credentials after `pnpm demo:reset`.
# Live adapters require all values below and are never used by unit tests.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
